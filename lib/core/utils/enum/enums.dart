//
// text sizes enum
enum TextSizes { small, medium, large }

// Order status
enum OrderStatus { processing, shipped, delivered }

// device type enum
enum DeviceType { mobile, tablet, desktop }

// Payment methods
enum PaymentMethods { paypal, googlePay, applePay, visa, masterCard, creditCard, payStack, razoPay, paytm }

